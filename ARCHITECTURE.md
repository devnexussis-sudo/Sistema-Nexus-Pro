# 📊 Diagrama da Arquitetura do Sistema

## 🏛️ Visão Geral da Arquitetura

```
┌─────────────────────────────────────────────────────────────────┐
│                         USUÁRIOS                                 │
│                                                                  │
│  👨‍💼 Admin    👷 Técnico    👤 Cliente (Público)                │
└───────────────────────┬──────────────────────────────────────────┘
                        │
                        ▼
┌─────────────────────────────────────────────────────────────────┐
│                    FRONTEND (React + Vite)                       │
│                                                                  │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐          │
│  │   Admin      │  │   Técnico    │  │   Público    │          │
│  │  Dashboard   │  │  Dashboard   │  │ Order View   │          │
│  └──────────────┘  └──────────────┘  └──────────────┘          │
│                                                                  │
│  ┌────────────────────────────────────────────────────┐         │
│  │         Components (UI, Forms, etc)                │         │
│  └────────────────────────────────────────────────────┘         │
│                                                                  │
│  ┌────────────────────────────────────────────────────┐         │
│  │              Services Layer                        │         │
│  │  • dataService.ts                                  │         │
│  │  • edgeFunctionService.ts                          │         │
│  │  • storageService.ts                               │         │
│  └────────────────────────────────────────────────────┘         │
└───────────────────────┬──────────────────────────────────────────┘
                        │
                        ▼
┌─────────────────────────────────────────────────────────────────┐
│                    SHARED LAYER                                  │
│                                                                  │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐          │
│  │    Types     │  │  Constants   │  │   Utils      │          │
│  │              │  │              │  │              │          │
│  │ • User       │  │ • Validation │  │ • formatCPF  │          │
│  │ • Order      │  │ • Status     │  │ • formatDate │          │
│  │ • Customer   │  │ • Priorities │  │ • validators │          │
│  └──────────────┘  └──────────────┘  └──────────────┘          │
└─────────────────────┬────────────────────────────────────────────┘
                      │
        ┌─────────────┴─────────────┐
        ▼                           ▼
┌──────────────────┐      ┌──────────────────────┐
│  Direct Access   │      │   Edge Functions     │
│  (Supabase SDK)  │      │   (Backend Logic)    │
│                  │      │                      │
│  • Simple CRUD   │      │  • get-orders        │
│  • Real-time     │      │  • create-order      │
│  • Storage       │      │  • update-order      │
└────────┬─────────┘      └──────────┬───────────┘
         │                           │
         └───────────┬───────────────┘
                     ▼
┌─────────────────────────────────────────────────────────────────┐
│                    SUPABASE BACKEND                              │
│                                                                  │
│  ┌────────────────────────────────────────────────────┐         │
│  │              PostgreSQL Database                   │         │
│  │                                                    │         │
│  │  Tables:                                           │         │
│  │  • tenants         • equipment_families            │         │
│  │  • users           • equipments                    │         │
│  │  • orders          • form_templates                │         │
│  │  • customers       • audit_logs                    │         │
│  └────────────────────────────────────────────────────┘         │
│                                                                  │
│  ┌────────────────────────────────────────────────────┐         │
│  │         Row Level Security (RLS)                   │         │
│  │  • Isolamento por tenant                           │         │
│  │  • Políticas de acesso                             │         │
│  └────────────────────────────────────────────────────┘         │
│                                                                  │
│  ┌────────────────────────────────────────────────────┐         │
│  │              Supabase Auth                         │         │
│  │  • JWT Tokens                                      │         │
│  │  • User Management                                 │         │
│  └────────────────────────────────────────────────────┘         │
│                                                                  │
│  ┌────────────────────────────────────────────────────┐         │
│  │              Supabase Storage                      │         │
│  │  • Fotos de equipamentos                           │         │
│  │  • Assinaturas                                     │         │
│  │  • Anexos de OS                                    │         │
│  └────────────────────────────────────────────────────┘         │
└─────────────────────────────────────────────────────────────────┘
```

---

## 🔄 Fluxo de Dados

### Cenário 1: Buscar Ordens (Simples)

```
Frontend
  │
  ├─ dataService.getOrders()
  │
  └─► Supabase Client
       │
       └─► PostgreSQL
            │
            └─► RLS valida permissões
                 │
                 └─► Retorna dados filtrados
```

### Cenário 2: Criar Ordem (Com Lógica de Negócio)

```
Frontend
  │
  ├─ edgeFunctionService.orders.create()
  │
  └─► Edge Function (create-order)
       │
       ├─ Validações
       ├─ Gera ID automático (OS-2026-0001)
       ├─ Busca config do tenant
       ├─ Cria ordem no DB
       └─ Cria log de auditoria
            │
            └─► PostgreSQL
                 │
                 └─► Retorna ordem criada
```

### Cenário 3: Upload de Foto

```
Frontend
  │
  ├─ storageService.uploadPhoto()
  │
  └─► Supabase Storage
       │
       ├─ Valida tamanho/tipo
       ├─ Upload do arquivo
       └─ Retorna URL pública
            │
            └─► Anexa URL na ordem
```

---

## 🎯 Decisão: Quando Usar Cada Camada?

### ✅ Use Supabase Client Direto

```typescript
// Operações simples de CRUD
const { data } = await supabase
  .from('orders')
  .select('*')
  .eq('status', 'PENDENTE')
```

**Quando:**
- CRUD simples
- Leitura de dados
- RLS já protege adequadamente
- Não precisa de lógica complexa

### ✅ Use Edge Functions

```typescript
// Operações complexas
const result = await edgeFunctionService.orders.create({
  title: 'Nova Ordem',
  // ... dados
})
```

**Quando:**
- Geração de IDs complexos
- Validações de negócio
- Integração com APIs externas
- Logs de auditoria
- Processamento pesado

---

## 📁 Mapeamento de Arquivos

```
Frontend Components → Supabase Tables
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

AdminDashboard.tsx    →  orders, users
TechDashboard.tsx     →  orders (assigned)
CustomerManagement    →  customers
TechnicianManagement  →  users (role=TECH)
EquipmentManagement   →  equipments, families
FormManagement        →  form_templates
OrderDetailsModal     →  orders, form_data
PublicOrderView       →  orders (public)
```

---

## 🔐 Camadas de Segurança

```
Layer 1: Frontend Validation
  ↓ (UX - feedback rápido)

Layer 2: Edge Function Validation
  ↓ (Servidor - validação real)

Layer 3: Row Level Security (RLS)
  ↓ (Database - último guardião)

Layer 4: Supabase Auth
  ↓ (JWT - autenticação)

✅ Dados Seguros
```

---

## 📊 Estrutura de Permissões

```
┌─────────────────────────────────────────┐
│          SUPER ADMIN                     │
│  ✓ Gerenciar tenants                     │
│  ✓ Todas as permissões                   │
└────────────────┬────────────────────────┘
                 │
        ┌────────┴────────┐
        ▼                 ▼
┌─────────────┐    ┌──────────────┐
│    ADMIN    │    │  TÉCNICO     │
│ ✓ Orders    │    │ ✓ Ver OS     │
│ ✓ Customers │    │ ✓ Atualizar  │
│ ✓ Equips    │    │ ✓ Checklist  │
│ ✓ Techs     │    │ ✗ Deletar    │
│ ✓ Settings  │    │ ✗ Gerenciar  │
└─────────────┘    └──────────────┘
```

---

## 🚀 Deploy Flow

```
1. Development
   ├── npm run dev
   └── localhost:3000

2. Build
   ├── npm run build
   └── /dist

3. Deploy Frontend
   ├── Vercel / Netlify
   └── https://app.nexuspro.com

4. Deploy Backend (Edge Functions)
   ├── supabase functions deploy
   └── Supabase Cloud

5. Database
   ├── Supabase Dashboard
   └── Run migrations
```

---

## 📈 Escalabilidade

```
Pequeno  (< 100 OS/mês)
  └─► Supabase Free Tier
       └─► Funciona perfeitamente

Médio    (100-1000 OS/mês)
  └─► Supabase Pro
       └─► + Edge Functions
            └─► + Storage

Grande   (1000+ OS/mês)
  └─► Supabase Pro
       └─► + Database Optimization
            └─► + CDN
                 └─► + Caching
```

---

## 🎨 Tech Stack Resumida

```
┌──────────────────────────────────────┐
│  Frontend                             │
│  • React 19                           │
│  • TypeScript                         │
│  • Vite                               │
│  • Lucide Icons                       │
└──────────────────────────────────────┘
           │
           ▼
┌──────────────────────────────────────┐
│  Backend                              │
│  • Supabase                           │
│  • PostgreSQL                         │
│  • Edge Functions (Deno)              │
│  • Row Level Security                 │
└──────────────────────────────────────┘
           │
           ▼
┌──────────────────────────────────────┐
│  Infrastructure                       │
│  • Vercel/Netlify (Frontend)         │
│  • Supabase Cloud (Backend)          │
│  • Git (Version Control)             │
└──────────────────────────────────────┘
```

---

**Este diagrama representa a arquitetura completa do Nexus Pro** 🎯
